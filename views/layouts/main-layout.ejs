<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC"
      crossorigin="anonymous"
    />
    <style>
      .scroll-container {
        overflow-x: auto; /* Enable horizontal scrolling */
        white-space: nowrap; /* Prevent wrapping */
        padding: 20px; /* Optional: add padding */
        position: relative; /* Positioning for arrows */
      }

      .card {
        display: inline-block; /* Keep cards in a horizontal row */
        width: 18rem; /* Set width for each card */
        margin-right: 20px; /* Spacing between cards */
      }

      .scroll-indicator {
        display: flex;
        justify-content: center;
        margin-top: 10px;
      }

      .dot {
        height: 10px;
        width: 10px;
        margin: 0 5px;
        background-color: #ccc;
        border-radius: 50%;
        display: inline-block;
      }

      .actives {
        background-color: #007bff; /* Active dot color */
      }
      .scroll-container::-webkit-scrollbar {
        display: none; /* Hides scrollbar in WebKit browsers (Chrome, Safari) */
      }

      .scroll-container {
        -ms-overflow-style: none; /* Hides scrollbar in Internet Explorer and Edge */
        scrollbar-width: none; /* Hides scrollbar in Firefox */
      }

      .about-alga-container .for-image img {
        max-width: 100%; /* Membuat gambar responsif */
        height: auto; /* Mempertahankan aspek rasio gambar */
        display: block; /* Menghindari spasi di bawah gambar */
        margin: 0 auto;
      }

      .container-favian .card {
        height: 350px; /* Atur tinggi card sesuai kebutuhan */
        width: 300px;
      }
    </style>

    <title>Fipoxygen | <%= title %></title>
    <link rel="icon" type="image/jpg" href="img/SAVE1.jpg" />
  </head>
  <body>
    <%- include('nav') %>
    <div class="container"><%- body %></div>

    <footer class="bg-success text-white text-center py-3 mt-3">
      <div class="container">
        <p class="mb-0">© 2024 Fipoxygen. All Rights Reserved.</p>
        <ul class="list-unstyled d-flex justify-content-center">
          <li class="mx-2">
            <a href="#" class="text-white">Privacy Policy</a>
          </li>
          <li class="mx-2">
            <a href="#" class="text-white">Terms of Service</a>
          </li>
          <li class="mx-2"><a href="#" class="text-white">Contact Us</a></li>
        </ul>
      </div>
    </footer>

    <!-- Optional JavaScript; choose one of the two! -->

    <!-- Option 1: Bootstrap Bundle with Popper -->
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
      crossorigin="anonymous"
    ></script>
    <script>
      const scrollContainer = document.getElementById("scroll-container");
      const dots = document.querySelectorAll(".dot");
      let currentIndex = 0;

      document.getElementById("scroll-left").addEventListener("click", () => {
        if (currentIndex > 0) {
          currentIndex--;
          scrollContainer.scrollBy({
            left: -scrollContainer.offsetWidth,
            behavior: "smooth",
          });
          updateDots();
        }
      });

      document.getElementById("scroll-right").addEventListener("click", () => {
        if (currentIndex < dots.length - 1) {
          currentIndex++;
          scrollContainer.scrollBy({
            left: scrollContainer.offsetWidth,
            behavior: "smooth",
          });
          updateDots();
        }
      });

      function updateDots() {
        dots.forEach((dot, index) => {
          dot.classList.toggle("actives", index === currentIndex);
        });
      }
    </script>

    <!-- Option 2: Separate Popper and Bootstrap JS -->
    <!--
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.min.js" integrity="sha384-cVKIPhGWiC2Al4u+LWgxfKTRIcfu0JTxR+EQDz/bgldoEyl4H0zUF0QKbrJ0EcQF" crossorigin="anonymous"></script>
    -->
  </body>
</html>
